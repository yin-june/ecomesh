# --- Core Web Framework ---
# High-performance asynchronous web framework and server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9      # Required by FastAPI for OAuth2 form parsing (Login)

# --- Configuration & Data Validation ---
# Pydantic v2 for robust schema validation and environment variable management
pydantic>=2.6.0
pydantic-settings>=2.2.0

# --- Security & Authentication ---
# JWT token generation and password hashing
passlib[bcrypt]>=1.7.4       # Industry-standard password hashing
python-jose[cryptography]>=3.3.0 # JSON Web Token encoding/decoding

# --- Relational Database (PostgreSQL) ---
# Object Relational Mapper (ORM) and Postgres driver
SQLAlchemy>=2.0.28
psycopg2-binary>=2.9.9

# --- Time-Series Database (InfluxDB) ---
# High-frequency sensor telemetry storage
influxdb-client>=1.41.0

# --- Machine Learning & Data Science ---
# The "Brain" of EcoMesh for Demand Forecasting & NILM
pandas>=2.2.1                # Data manipulation and cleaning
numpy>=1.26.4                # Numerical operations and array variance math
scikit-learn>=1.4.1          # Random Forest Regressor and AI pipelines
joblib>=1.3.2                # Saving and loading trained .pkl AI models

# --- IoT & External Communication ---
# Hardware bridging and 3rd-party REST API ingestion
paho-mqtt>=1.6.1             # MQTT protocol for ESP32 Gateway communication
httpx>=0.27.0                # Async HTTP client for the MetMalaysia API integration

# --- Utility & Testing ---
python-dotenv>=1.0.1         # Fallback for local .env file parsing
pytest>=8.1.1                # Automated testing suite
pytest-asyncio>=0.23.5       # Testing asynchronous endpoints